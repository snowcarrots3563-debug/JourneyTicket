# JourneyTicket 0.5.2 UI 重构暂停交接

> 暂停时间：2026-08-30（Asia/Shanghai）  
> 项目目录：`E:\claude work place\旅程记录\JourneyTicket`  
> 当前状态：已按用户要求暂停，所有本轮子代理均已终止，没有后台开发任务继续运行。

## 1. 本轮任务目标

根据项目根目录下五份 HTML Demo，将 Android Jetpack Compose 应用重构为 A「白昼纸张」视觉方向：

1. `ui-design-v0.5.2-demo.html`
2. `ui-design-v0.5.2-capture-demo.html`
3. `ui-design-v0.5.2-confirm-demo.html`
4. `ui-design-v0.5.2-timeline-demo.html`
5. `ui-design-v0.5.2-settings-demo.html`

核心约束：纪念票内容、结构、布局和渲染器已经封存，禁止修改 `app/src/main/java/com/journeyticket/render/`。

## 2. 已完成工作

### 2.1 代码库与 Demo 映射分析

- 首页 Demo → `ui/home/HomeScreen.kt`
- 识图 Demo → `ui/capture/CaptureScreen.kt`
- 确认 Demo → `ui/confirm/ConfirmScreen.kt`
- 旅途 Demo → `ui/timeline/TimelineScreen.kt`
- 设置 Demo → `ui/settings/SettingsScreen.kt`
- 票面预览外围 UI → `ui/preview/TicketPreviewScreen.kt`
- 全局主题 → `ui/theme/Color.kt`、`ui/theme/Theme.kt`
- 顶层导航整合 → `ui/navigation/AppNavHost.kt`、`Destinations.kt`

### 2.2 已落地并验证的代码

共享主题 worker 已完成并通过 Kotlin 编译：

- `app/src/main/java/com/journeyticket/ui/theme/Color.kt`
- `app/src/main/java/com/journeyticket/ui/theme/Theme.kt`

已实现：

- A「白昼纸张」浅色语义色
- 完整深色语义色
- 铁路蓝主色 `#2F66B0`
- Material3 ColorScheme
- 统一排版层级
- 12dp / 18dp / 24dp 圆角体系
- 保留 `BrandBlue`、车次颜色及 `JourneyTicketTheme` 公共接口
- 系统深色模式继续有效

验证结果：

```text
:app:compileDebugKotlin
BUILD SUCCESSFUL in 19s
```

仅出现项目原有 Android Gradle Plugin 配置警告，不影响编译。

### 2.3 已生成的安全备份

本轮开始修改前，以下 UI 文件已备份到：

`C:\Users\fdcx\Documents\ChatGPT\旅行项目\code-backup-before-five-demo-ui`

备份包含主题、首页、识图、确认、旅途、设置、预览和导航文件。项目本身没有 `.git`，如需回退应使用该备份逐文件恢复，禁止整目录覆盖。

## 3. 暂停时精确文件状态

与本轮修改前备份比较：

### 已修改

| 文件 | 当前 SHA-256 | 状态 |
|---|---|---|
| `ui/theme/Theme.kt` | `A4DFB7CD928895BA77283D409E2EC104804ED4AEFC1A6488B8A40A59C3480A8F` | 主题 worker 完成，已编译通过 |
| `ui/theme/Color.kt` | `447B14977A3AFBB45FCA78BFB1673C2B4CEF3803EF904DFCC3740F0C8E05D867` | 主题 worker 完成，已编译通过 |

### 未修改，仍与暂停前备份完全一致

| 文件 | SHA-256 |
|---|---|
| `ui/home/HomeScreen.kt` | `380D62DAFCD1E5642511B229FA4FC27DD44F5F058E2DC8D9245B00063B8B52CE` |
| `ui/capture/CaptureScreen.kt` | `E009C4AB296E0694283B7C5DD458E8899B15419E819C636DA4FAB69BFDA32C5A` |
| `ui/confirm/ConfirmScreen.kt` | `16BF4E9D8976F914D6C89BA3B87D1C75B8DD2E825D0F15614E6F59094684CE2C` |
| `ui/timeline/TimelineScreen.kt` | `F5DA8B7428FCF5BAC825DE48C75680B3DB7995AC66EF936EB4B99CB2408B9C2C` |
| `ui/settings/SettingsScreen.kt` | `8470F8F8D79608C2B91A7F664BDF52B4397FB3376C165472888A2614425D5B06` |
| `ui/preview/TicketPreviewScreen.kt` | `706E0C9420E28501BC5F79559A62041A3138C3AFCAE917752B2E5AA7A5EF231C` |
| `ui/navigation/AppNavHost.kt` | `0D42E29656D0CE4101C82BC32FCC6C0B0483FC04281A8DB9CC968C73053FF095` |
| `ui/navigation/Destinations.kt` | `C4F93858CC41BE77C06515F70C55102655B9B344BF017FAA81E2BD2DC8CC8712` |

结论：五个页面 worker 在写入代码前均被终止，没有留下半成品页面文件。

## 4. 已终止的子代理

以下 worker 均在暂停指令后立即关闭，状态为 `shutdown`：

| 代理 | 原负责范围 | 暂停结果 |
|---|---|---|
| Copernicus | `HomeScreen.kt`、`TicketPreviewScreen.kt` | 未写入 |
| Bernoulli | `CaptureScreen.kt` | 未写入 |
| Aristotle | `ConfirmScreen.kt` | 未写入 |
| Nietzsche | `TimelineScreen.kt` | 未写入 |
| Mencius | `SettingsScreen.kt` | 未写入 |

没有仍在运行的本轮子代理。

## 5. 票面封存完整性

暂停时重新校验，`render` 包哈希与本轮启动前基线完全一致：

| 文件 | SHA-256 |
|---|---|
| `BitmapSanity.kt` | `7CFB5041B348E54C66AEF246A50C8AA19433245A96FF8ECEFCC765262AAA280D` |
| `PngExporter.kt` | `CC0672DEC484A3F19F3AC2F708EB5994488C323674A1787F01C1AC2B5829625E` |
| `QrCodeGenerator.kt` | `9956C182D9FBE9A75CBA296C1E8734BA477ACB0B7DEB474334D122E278AC4AD5` |
| `RenderDebugDumper.kt` | `3E3CCE04285948C2153FE727EBB43A9675B233F107BE0086FAB2B57F898816F7` |
| `RenderDimensions.kt` | `412E4B433F3AEC778A25D3579192729766C1EC927D1E46093A3E0134CA676A8B` |
| `TicketBackgroundProvider.kt` | `17DE5854AF3EEE819E73CC0823F0760BBE9A1D6CF41AD8D52E25419DA516832E` |
| `TicketRenderer.kt` | `9B763E21CCB120165CBCD2C09E64D567F9B81E39260869FA08D26AA87C3C0F68` |
| `TicketRendererImpl.kt` | `DB139DFBE2916C4F77BF0CFB7CFA6A531DE8860DE44923627F5FFE5CD917F2A5` |

恢复开发后必须再次比对这些哈希；任意变化都应立即停止合并。

## 6. 明天恢复开发的推荐顺序

### 阶段 A：重新启动互不冲突的页面 workers

1. 首页与预览：只允许修改 `HomeScreen.kt`、`TicketPreviewScreen.kt`
2. 识图：只允许修改 `CaptureScreen.kt`
3. 确认：只允许修改 `ConfirmScreen.kt`
4. 旅途：只允许修改 `TimelineScreen.kt`
5. 设置：只允许修改 `SettingsScreen.kt`

页面 worker 不得修改 ViewModel、导航、主题、Gradle、文档或 `render` 包。

### 阶段 B：顺序整合导航

等待所有页面签名稳定后，由单一 worker 修改：

- `ui/navigation/AppNavHost.kt`
- 必要时 `ui/navigation/Destinations.kt`

已确定的导航规则：

- 底部导航只显示在“首页 / 旅途 / 设置”三个顶层页面。
- 识图、确认、预览为沉浸式流程页，不显示底部导航。
- 保留识图 → 确认 → 预览链路。
- 保留旅途 → 预览回看链路。
- 使用 `launchSingleTop` 和合理的 `popUpTo`，避免顶层页面重复堆栈。

### 阶段 C：评审与验证

由只读 reviewer 对照五份 HTML 检查：

- 深浅色主题
- 360dp 宽度和系统字体放大
- 48dp 触控目标
- TalkBack 描述
- 加载、空、错误和禁用状态
- 原业务回调是否全部保留
- `render` 包哈希是否不变

最终依次执行：

```powershell
$env:JAVA_HOME='C:\Users\fdcx\build-tools\jdk-17.0.20.1+1'
.\gradlew.bat :app:compileDebugKotlin --no-daemon
.\gradlew.bat :app:testDebugUnitTest --no-daemon
.\gradlew.bat :app:lintDebug --no-daemon
.\gradlew.bat :app:assembleDebug --no-daemon
```

## 7. 功能边界与风险

- HTML 中“手动记录”没有现有路由和业务页面，不得做成空按钮。
- 时间线现有数据只支持真实总次数和日期分组；不得伪造里程、城市数、筛选器或状态。
- 设置页现有真实功能是 API 地址、API Key、模型、JSON mode、DPI、连接测试和日志导出。
- HTML 中手动主题、文字大小、自动保存识别图片、备份等尚无持久化业务支持，不得伪装为可用开关。
- 确认页必须保留 15 项字段、异常字段提示、证件号历史、重新识别、仅生成和保存存档。
- 预览页只允许调整外围 UI；票面位图、85:54 比例、保存与重新生成逻辑不能改变。
- 项目没有 Git；任何大范围修改前都应继续做明确文件快照。

## 8. 当前计划状态

- [x] 分析代码库与五份 HTML
- [x] 建立票面渲染哈希基线
- [x] 备份本轮 UI 文件
- [x] 完成共享主题与设计 tokens，并通过 Kotlin 编译
- [ ] 实现首页/预览 UI
- [ ] 实现识图 UI
- [ ] 实现确认 UI
- [ ] 实现旅途 UI
- [ ] 实现设置 UI
- [ ] 整合顶层底部导航
- [ ] 运行单元测试、lint 与 APK 构建
- [ ] 模拟器逐页视觉验收

